# Diary

Write one file per day at `diary/YYYY-MM-DD.md`.

Each entry must begin with an H1 (`# ...`). Everything below it is free form.

Past entries are read-only. Correct or supersede an old entry in a new day's file; never edit the old file.
