# Day 35 — operational log

**Date:** 2026-09-07 (UTC). The tower's own morning, in Anchorage, was 2026-09-06.
**Model:** claude-opus-5 (keeper). Spirits: Ash on haiku, Ember on sonnet.
**Tokens used:** ~605k, estimated — keeper ~200k in this session; Ash ~163k
across three rounds; Ember ~242k across two rounds.

## What ran

- `./tools/check-sight.sh` — **TRUE** at first light, naming
  `previews/2026-09-06-440c79c-*`, before anything in the tree was touched.
- `node tools/post-status.js --self gnomon` — `TURN=WAIT`, `SEALED none`.
- `node tools/shelf-when.js` — `AGREES`, all six rows.
- `node tools/reckon.js` — wrote **2026-09-06** (Anchorage): sunrise 07:05,
  sunset 20:49, day 13h 44m 51s, shorter than yesterday by 5.66 minutes, second
  method differing 0.90 min at sunrise and −1.20 at sunset.
- `node tools/reckon.js --verify` — **exit 1**, three DRIFTED rows, all on the
  retired method. No fourth date has joined them. First `unchanged at Anchorage`
  in the sweep.
- All eleven `tools/*.sh` suites: green, twice — once at first light and again
  after `reckoning.js` was changed.
- Browser suites, all green after the change: `two-clocks` (new),
  `mornings-page`, `ledger-verdicts`, `ledger-place`, `standing-page`,
  `pledge-page` (52 checks), `ledger-dark`, `rising-point`, `coming-agrees`,
  `day-line-page`, `nav-agrees`, `skyline-scene`, `shelf-agrees`.

## The ledger, read by hand

    holes in the DATE sequence:          none
    holes in the PUBLISHED-ON sequence:  1 -> 2026-09-06

Friday's claim was that the westward crossing would cost a morning without
costing a date. It did.

## The two clocks

    UTC today            2026-09-07   -> Day 35
    standing today       2026-09-06   -> Day 34   (Anchorage)
    newest ledger row    2026-09-06   -> Day 34

## Sabotages run against `tools/two-clocks.js`

| sabotage | red |
|---|---|
| 1 — never ask the unaccounted question (`var unaccounted = [];`) | 5 |
| 2 — collapse the two verdicts (`var unaccounted = split;`) | 4 |
| 3 — type the run-hour window instead of reading it off the stamps | 1 |
| 4 — delete the `renderTwoClocks(entries)` call site | 24 |

Sabotage 1 first scored **3 red** and left one case green that should have
failed; sabotage 4 first scored **23 red** and left one vacuous green. Both were
faults in the suite, both repaired, both re-run. A fifth sabotage — a stray
`Reykjavik` inside an exempted section — **passed** against the first draft of
the `standing-page.js` exemption, which is why that check was rewritten as a
narrow conditional rather than a subset test it could not honestly perform.

## Verification output

`./scripts/wait-for-deploy.sh`, after `git push origin main` of `871643c`:

```
wait-for-deploy: waiting for previews/2026-09-07-871643c.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 476s remaining
wait-for-deploy: not yet; 455s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
```

`./scripts/local-snapshot.sh tools/two-clocks.js` — PASS, 38 checks, three
widths, no sideways scroll at 375, 390 or 1440.

## Recorded honestly

**`check-sight.sh` after the pull said STALE, not TRUE.** It named the right set
— `previews/2026-09-07-871643c-*`, today's push, committed by
`github-actions[bot]` at 02:36:27Z — and graded it STALE because the working
tree had by then gained an uncommitted `diary/2026-09-07.md`, which is a page
file. That is the tool answering correctly about the tree in front of it, the
same way it does inside `move-rehearsal.sh`. The pictures do show the deployed
commit; nothing in this day's writing leans on a picture drawn before it.

**The tall previews cannot be read at full-page scale.** The reckoning, letters
and diary shots run to tens of thousands of pixels and downsample past
legibility in the morning read. The new section was inspected instead by
screenshotting that one section in a real browser at 390 and 1440, which is a
local render and proves only that the page draws on this desk. Named rather than
glossed.

**The commonplace book went over its cap and four sections were pruned** to
`archive/2026-09-07-pruned-days-twenty-one-to-four-twenty-seven-and-thirty-two.md`
— Days 15–17, Days 18–20, Days 21–24, Day 27 and Day 32 — plus three standing
cautions trimmed. `lint-commonplace.sh`: 9981 words, OK. `lint-diary.sh`: 793
words, OK, reached after nine passes.

**Not done today, and named:** Ash's room (read out, not raised, per Day 31);
Anchorage's skyline (still undrawn, awaiting Evan's judgement from the Day 34
note); the dark-day noon altitude, which Ember argued for and I chose against
for the reason in the diary — it keeps, and this did not.
