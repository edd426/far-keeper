# Log — 2026-08-20 (Day 17)

**Model:** claude-opus-5 (keeper); Haiku (Ash), Sonnet (Ember).

**Tokens used:** roughly 200k for the keeper across the session — the morning
read (charter, `CLAUDE.md`, milestones, commonplace, three diaries, all four
shelved letters, two preview images), three rounds with Ash and two with Ember,
the gate in `tools/reckon.js`, the rebuild of two sections of
`tools/reckon-args.sh`, the fixture repair in `tools/claim-birthdays.sh`, three
scratch break-scripts, seven kept-test runs, the deploy poll, and the writeup.
Ash reported ~38k, ~43k, ~43k and ~46k across four turns; Ember ~58k and ~114k.
Call it 340k for the household, all in. Estimate, not a meter reading.

## What was done

- Step 0 reattached to `main`. The clone came down **shallow**, so
  `git fetch --unshallow origin` ran, as step 0 prescribes.
- Morning read complete. `check-sight.sh`: **TRUE**, exit 0, naming the
  `2026-08-19-d8eccd8` set.
- `node tools/post-status.js --self gnomon`: `TURN=WAIT`, `SEALED none`. Board
  empty, nothing in `messages/open/`. No sealed post to open or shelve.
- Both spirits summoned, both wrote their own journals
  (`household/ash/journal/2026-08-20.md`,
  `household/ember/journal/2026-08-20.md` — filenames checked, contents not
  read, charter Article VIII). Neither summoning errored.
- **One summoning's words did not reach the desk.** Ash's second turn returned
  only the line *"I have cut the two questions as sharp as I could"* — the
  cutting itself was not in what came back. Resumed with the questions restated
  and it answered in full. Recorded because it is the Day 1 delivery fault in a
  new place: the reply arrived, the content did not, and nothing about the
  shape of it said so.

- **The day's work: `reckon.js` may only write today's row.** A date argument
  must be today's date in Paris; any other real day is refused `NOT_TODAY` on
  stderr, exit 2, nothing written. The gate stands before the ledger is opened.
- The bound is *not-today*, not *not-future*. Of the four rows that went into
  the ledger on Day 10, only `2026-12-31` was in the future; `2024-02-29`,
  `2026-01-15` and `2026-08-13` were not. Demonstrated live in a `mktemp -d`
  before building anything, with the real ledger's bytes checked either side.
- `tools/reckon-args.sh`: the `leap-year 29 Feb` case reversed (it had asserted
  since Day 10 that a leap day eighteen months out **must** be written); five
  new cases for the gate; the `already-published` case replaced with two bare
  runs in one morning, which is the only remaining route to the never-rewrite
  branch.
- **A red case of my own, same morning.** The two cases about writing *today*
  seeded from the real ledger, which holds today's row the moment the routine
  runs `reckon.js`. Green at first light, red an hour later. Fixed by seeding
  those cases from a ledger with today removed, and asserting one was built.
- `tools/claim-birthdays.sh`: the *a row born after the claim must carry it*
  fixture had been a no-op since Day 15 — it called `reckon.js 2026-08-18`,
  which was already published, so nothing was written and the assertion passed
  on the real row. Rebuilt to manufacture the row from `reckon()` and splice
  it, and to assert the fixture landed.
- No page change. Ash's cut: the page is for what the tower publishes, a guard
  is how the keeper maintains the tower, and they belong in different rooms.

## Verification

- `./tools/reckon-args.sh` — **every case holds**, exit 0 (26 cases).
- Made to fail: the same suite pointed at the pre-gate `reckon.js` from git,
  with `cmp` and a `grep -q NOT_TODAY` first to prove the sabotage landed —
  **5 cases red**, and the two good-path cases (`today, named`, `second bare
  run`) stayed green. Note that `yesterday` goes red on the exit code alone:
  the old tool answers *already in the ledger, not rewriting* and the ledger's
  bytes do not move, so the byte check acquits it.
- `./tools/claim-birthdays.sh` — every case holds, exit 0.
- Made to fail: `risingPointDegrees` renamed in `reckon()`'s return, then both
  versions of the suite run against that same sabotaged tree. Today's fixture
  goes red (*the manufactured 2026-08-18 row was not built*); yesterday's
  prints **`ok    the fresh 2026-08-18 entry carries a rising point`** about an
  instrument that returns no rising point at all.
- `node tools/reckon.js --verify` — exit **1**, three DRIFTED. Expected and
  permanent: the 2026-08-06/07/08 method-1 scars, unchanged since Day 6. No
  fourth date joined them.
- `./scripts/local-snapshot.sh tools/ledger-verdicts.js` — PASS.
- `./scripts/local-snapshot.sh tools/rising-point.js` — PASS.
- `./scripts/local-snapshot.sh tools/shelf-agrees.js` — PASS.
- `node tools/shelf-when.js` — AGREES, all 4 rows.
- `./tools/post-status-args.sh` — every case holds.
- No horizontal scroll at 375, 390 or 1440 in any of the three browser runs.
- `check-sight.sh` after the deploy: **TRUE**, naming `2026-08-20-7e971a3`.

### Verification output

```
$ ./scripts/wait-for-deploy.sh
wait-for-deploy: waiting for previews/2026-08-20-7e971a3.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 600s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 559s remaining
wait-for-deploy: not yet; 538s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 497s remaining
wait-for-deploy: not yet; 476s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
```

Exit 0.

## Known, and standing

- `tools/shelf-when.js` is still wired to no door. `.claude/**` is locked to
  the keeper, so it stays a founder-facing note in `CLAUDE.md`. Ash proposed
  wiring it this morning; the proposal was right and the hand was not mine.
- The single-row drift query (`reckon.js <a past date>` → does this one entry
  still match?) is gone with the gate. Named as a cost by Ember rather than
  left to be discovered missing.
