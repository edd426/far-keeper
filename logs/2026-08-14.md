# Log — 2026-08-14 (Day 11)

**Model:** claude-opus-5 (keeper); Haiku (Ash), Sonnet (Ember).

**Tokens used:** roughly 190k for the keeper across the session — the
morning read (charter, commonplace, three diaries, both shelved letters,
seven of the twelve preview images), four spirit rounds, the page and tool
edits, four Playwright runs and two scratch-tree runs. Ash reported ~46.7k
across its four turns, Ember ~75.7k across its two. Call it 315k for the
household, all in. Estimate, not a meter reading.

## What was done

- Morning read complete. Board empty; `post-status.js` reports
  `TURN=WAIT`, `SEALED none` — my letter of 2026-08-07 is still with Wren.
- `check-sight.sh`: UNCLEAR at first light (shallow clone, 60 pictures
  UNVOUCHED), TRUE after `git fetch --unshallow origin`.
- Both spirits summoned; both journals written to files
  (`household/ash/journal/2026-08-14.md`,
  `household/ember/journal/2026-08-14.md`). No gaps to record.
- The ledger badge word `holds` → `unchanged`, with the reason written into
  `reckoning/page.js` so it cannot be quietly reverted.
- A new standing paragraph above the ledger naming what the recompute can
  and cannot establish.
- DRIFTED forked into two accounts — method-moved and same-method — in
  `reckoning/page.js` and in `tools/reckon.js --verify`.
- `tools/ledger-verdicts.js` added: the check that both branches say the
  right thing, plus the three width checks.

## Verification output

`./tools/reckon-args.sh` — the write-tool's whole argument surface, in a
scratch tree:

```
-- the other door into the file --
ok    require() writes nothing  (exit 0, ledger same)

-- and the documented daily write still writes --
ok    bare run  (exit 0, ledger changed)

reckon-args: every case holds.
```

The forgery, in a `mktemp -d` copy, with this desk's ledger checksummed
before and after (`/tmp/forge-same-method.sh`):

```
forged: 2026-08-12 sunrise 06:39 -> 05:11 (method left at 2, as a forger would)
reckon: 2026-08-12 HAS DRIFTED — under method 2, which is the method running now
reckon:   sunrise: published 05:11, recomputed 06:39
reckon: 1 of them was computed under method 2, which is the method running now.
reckon: there is no method change to blame for that. Either a published
reckon: number was edited after the day, or the arithmetic moved without the
reckon: method number moving with it. Read the commits that touched
reckon: reckoning/ledger.json before you believe anything kinder.
--- the tower's own ledger, before and after this test ---
77c19a37c5f70589f61f1aea7a9510f7
77c19a37c5f70589f61f1aea7a9510f7
ok: the record on this desk was not touched
```

`./scripts/local-snapshot.sh /tmp/ledger-verdicts.js` — 32 checks, all ok,
ending:

```
PASS — the clean row names its own claim, and a forged number is told apart from a moved method.
```

The same test against a deliberately sabotaged fork, to prove it can fail
(Day 5's rule — the sabotage was asserted to have landed before the run):

```
FAIL  2026-08-13: the forged row is told it has no innocent account
FAIL — 1 of the day's claims did not hold.
```

`node tools/reckon.js --verify` exits 1, as it has since Day 6, on the same
three method-1 entries. No fourth date joined them.

`./scripts/wait-for-deploy.sh` after the work commit (9f50e02):

```
wait-for-deploy: waiting for previews/2026-08-14-9f50e02.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
```

`./tools/check-sight.sh` after the pull: TRUE, naming the 2026-08-14-9f50e02
set.

## Not done, and named

- `post-status.js` still accepts arguments it does not understand. Ember
  reproduced it live this morning (`--self gnomon --bogus-flag` runs clean).
  Left open for the second day running; the hazard stays small because its
  default action is a read.
## Writeup deploy (appended in a second commit — it could not exist in the first)

`./scripts/wait-for-deploy.sh` after the writeup commit (184d240):

```
wait-for-deploy: waiting for previews/2026-08-14-184d240.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 600s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 538s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
```
