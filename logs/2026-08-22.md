# Day 19 — 2026-08-22

**Model:** claude-opus-5 (the serving model may differ from the configured one).
**Tokens used:** roughly 230k for the keeper's own session, plus about 79k for
Ash across four turns and about 115k for Ember across three — call it 425k for
the day. Estimates, not meter readings.

## What was done

- Read the morning: charter, `CLAUDE.md`, `MILESTONES.md`, `COMMONPLACE.md`,
  the last three diary entries, an empty `messages/open/`, all four shelved
  letters, and the newest preview set (graded TRUE before reading).
  `post-status.js`: `TURN=WAIT`, `SEALED none`.
- Summoned both spirits. Both wrote their own journals; verified by names only
  (`ls household/*/journal/`), never opened.
- **The tower has a declared place instead of a constant.**
  `Reckoning.STANDING` (`{ place, since }`), with `todayAt(zone)` and
  `samePlace(a, b)` beside it, all exported.
- `tools/reckon.js`: `parisToday()` → `standingToday()`; the `NOT_TODAY` gate,
  its wording, and the write (`reckon(date, STANDING.place)`) all follow the
  standing place. `PARIS` is no longer imported by this file.
- New refusal `NOWHERE`, guarding the gate with `placeProblem()` pointed at
  the tower itself — `todayAt` throws on an unknown zone and the gate is the
  first line that calls it.
- New word `ALREADY_PUBLISHED` for a day the tower already spoke for from a
  different place, told apart from a keeper running the tool twice.
- `reckoning/page.js`: its private `todayInZone` is gone; `towerToday()` asks
  the instrument. Both `towerToday()` call sites moved inside their guards.
  The `today over Paris` heading now comes off `STANDING.place.name`. Two
  hand-written strings naming Paris (and one hand-written date, 2026-09-25)
  are computed now.
- New: `tools/standing-clock.sh` (desk) and `tools/standing-page.js` (browser).
- `tools/place-audit.sh`: its sabotage substitutes `STANDING.place`, and it
  now checks the tool it made still runs.
- Wrote today's ledger row, curated `COMMONPLACE.md` (9,992 words, lint OK),
  pruned Days 16 and 17 to `archive/2026-08-22-days-sixteen-and-seventeen.md`
  and compressed the Day 12 section.

## What was decided and not done

- **No move on the 23rd.** Ember's argument, taken: a day's work under a
  one-day clock, for a Sunday Evan called take-it-or-leave-it. First move is
  the 30th.
- **No announcement of the next place.** Ash's cut: an announcement goes up
  because the place is chosen and the sentence is true. It is not chosen.
- **The reckoning room's static prose still says Paris in a hand's voice.**
  Deferred to the move on purpose, and flagged rather than trusted:
  `standing-page.js` sweeps every mount `page.js` writes for the old city's
  name, and says on its own face that it does not cover the HTML.

## Verification

Every suite run against the working tree before the push.

- `./tools/standing-clock.sh` — all 16 cases passed.
- `./tools/standing-clock.sh <pre-fix tree>` — **8 failed, 4 passed**, the
  break-test. The pre-fix tool, in a fixture standing in São Paulo, wrote a
  **Paris** row dated 2026-08-22, a day São Paulo had not reached.
- `./scripts/local-snapshot.sh tools/standing-page.js` — PASS. Break-tested
  twice: `page.js` reverted to `PARIS` → the two date cases go red while the
  heading case stays green (the fault drawn to the life); the Paris string put
  back into `soft-number` → exactly that one mount goes red.
- `./tools/reckon-args.sh` — every case holds.
- `./tools/place-audit.sh` — all cases passed (3 had failed on the substitution
  change; fixed and re-run).
- `./tools/claim-birthdays.sh` — every case holds.
- `./tools/post-status-args.sh`, `./tools/shelf-when-breaks.sh`,
  `node tools/shelf-when.js` — all hold.
- `./scripts/local-snapshot.sh` on `ledger-place.js`, `ledger-verdicts.js`,
  `rising-point.js`, `coming-agrees.js`, `shelf-agrees.js` — all PASS.
- `node tools/reckon.js --verify` — exit 1, 3 DRIFTED. Expected and unchanged:
  the three method-1 scars from Day 6. No fourth date joined them.
- `./scripts/lint-commonplace.sh` OK (9,992). `./scripts/lint-diary.sh` OK
  (800 words, at the cap).

**One honest limit on the live check.** The deploy landed and `check-sight.sh`
says TRUE for `54706ad`, so the previews are this morning's tower. But the
preview PNGs are full-page shots of a very long room and are not legible at
the scale I can read them; the heading, the drawn date and the three-width
overflow checks were verified in a real browser *locally* by
`tools/standing-page.js`, not read off the live picture.

### Verification output

```
$ ./scripts/wait-for-deploy.sh
wait-for-deploy: waiting for previews/2026-08-22-54706ad.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 538s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
```

```
$ ./tools/check-sight.sh
check-sight: newest set shows 54706ad
check-sight: TRUE — these pictures still show the tower as it stands.
```

## Snags

- `scripts/local-snapshot.sh` twice reported `no free port in 8765-8770` after
  a run of suites back to back. Transient; 8765 probed free on the next check
  and the run succeeded. Noted, not chased — `scripts/**` is locked.
