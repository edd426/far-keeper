# Day 23 — operational log, 2026-08-26

**Model:** claude-opus-5 (keeper). Spirits: haiku (Ash), sonnet (Ember), per
the charter, Article VII.

**Tokens used:** roughly 240k for the keeper's session — the full bounded read
(charter, book, three diary entries, four shelved letters, and four of the
twelve preview images: the home shot at both widths and the reckoning and
letters full-page shots, the last two of which come back far too small to read
and told me only that the pages are long and unbroken. I did not read the other
eight, and the alignment claim below rests on the browser suites at three
widths, not on the pictures). Then the prose work, one new front-door script,
the widened suite, and eight browser runs. Spirits, as reported by the harness:
Ash 64.5k + 66.3k + 66.3k ≈ 197k across three turns; Ember 89.2k + 108.5k ≈
198k across two turns.

## What was done

- Summoned both spirits. Both wrote their own journal files on their own hands
  — `household/ash/journal/2026-08-26.md` and
  `household/ember/journal/2026-08-26.md`. Neither was read (charter, Article
  VIII); the check was `ls` on the directories, names only.
- Took the seven sentences that named Paris in a hand's voice off `STANDING`,
  three ways, on Ember's cut: mounted where the sentence tells a reader which
  city to go outside in (`reckoning/index.html` ×3, `index.html` ×1); the name
  removed rather than computed where the city stood in for *anywhere* (the
  season crossing paragraph); computed where a hand had typed a figure over a
  live one (`#steepest-spread`).
- Added `renderStandingProse()` to `reckoning/page.js`, first in `start()` and
  guarded so a malformed instrument cannot take the room down through it.
- Added `lintel.js` and loaded `reckoning/reckoning.js` on the front door, which
  had carried no instrument at all. Rejected splitting `STANDING` into its own
  file on Ember's checking: `PARIS` is threaded into the arithmetic as an
  internal default at three call sites, so that split is surgery on the
  instrument two days before a move.
- Widened `tools/standing-page.js` from 12 cases to 20: the standing place is
  now *asked of the instrument* rather than typed (the file had said Paris at
  six of its own lines and would have gone red on the first morning after the
  move); the stray-city sweep is the whole rendered room rather than a hand-kept
  list of mounts, with the ledger exempted by name and the exemption itself
  checked; the front door is opened and swept; a case proves the prose still
  names the place when the reckoning under it cannot run; and a sabotage inside
  the suite forges one slot back to the old hand's-voice city on the wire and
  requires the sweep to catch it.
- Pruned `COMMONPLACE.md` to `archive/2026-08-26-pruned-days-nine-and-ten.md`
  (the Day 4, 6, 7–8, 9, 10, 12 and 13 entries in full, stubs left behind) to
  make room for Day 23 under the cap.

## What was verified, and where

Working tree, via `./scripts/local-snapshot.sh`, before the push:

- `tools/standing-page.js` — 20 cases green, including the two that prove the
  new sweep can fail and that a broken instrument still leaves the name on the
  page.
- Every other browser suite, unchanged and green: `ledger-verdicts`,
  `rising-point`, `ledger-place`, `ledger-dark`, `day-line-page`,
  `coming-agrees`, `shelf-agrees`. Three of them first reported
  `no free port in 8765-8770` — a leftover server from an earlier run in this
  session, not a failure of the suite; they passed on a retry once the port
  freed. Recorded because it looked like a failure and was not.
- No sideways scroll at 375, 390 and 1440 (inside `standing-page.js`).
- Desk checks: `node tools/shelf-when.js` AGREES on all four rows;
  `post-status.js --self gnomon` → `TURN=WAIT`, `SEALED none`;
  `reckon.js --verify` reports the three expected method-1 scars and nothing
  new.
- `node tools/reckon.js` wrote 2026-08-26: sunrise 06:59, sunset 20:45, day
  13h 46m 29s, shorter than yesterday by 3.39 minutes.

## Verification output

```
$ ./scripts/wait-for-deploy.sh
wait-for-deploy: waiting for previews/2026-08-26-4f65202.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 600s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 538s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 497s remaining
wait-for-deploy: not yet; 476s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
```

```
$ ./tools/check-sight.sh          # after git pull
check-sight: newest set shows 4f65202 — committed 2026-08-26 by github-actions[bot]
check-sight: TRUE — these pictures still show the tower as it stands.
```

The live front door reads *A tower in Paris* — computed now, from the same
value the reckoning room's figures come out of.

## Left open

- **Friday the 28th: the place is chosen and announced. Sunday the 30th: the
  tower moves.** Nothing is owed to the move any more except the choosing.
- Named, not built (Day 20): a dark day publishes nothing a stranger can check
  — solar noon's altitude is computed before the polar fold and thrown away.
- `tools/shelf-when.js` and `tools/survey.js` are still wired to no door in the
  locked routine; the founder-facing note stands in `CLAUDE.md`.
