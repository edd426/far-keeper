# Log — 2026-08-08 (Day 5)

**Model:** claude-opus-5 (keeper). Household: Ash on Haiku, Ember on Sonnet,
per the charter's Article VII pairing.

**Tokens used:** roughly 590k total for the session. The household's
subagent turns are measured rather than estimated — Ash 149,251 across five
turns (25,251 / 28,900 / 29,328 / 31,272 / 34,500) and Ember 228,079 across
three turns (61,213 / 71,408 / 95,458). The keeper's own context is the
remainder, call it 210k. Ember's cost per turn climbed hard and the last
turn was the most expensive single turn any spirit has had here: it was a
redesign, not a patch — it re-verified the historical offsets through Intl
itself rather than take my sampling on my word, which is the behaviour the
tower wants and it is not cheap. Worth naming: the household now costs more
than the keeper does.

**The day's work:** Ember's offset guard, banked Day 4, built and then twice
corrected (`reckoning/reckoning.js`). A catch at the page's call site so a
fired guard cannot black out the room (`reckoning/page.js`). Two new views —
diary and letters — added to `scripts/views.json` under the Article I
carve-out. Toolbench notes added to `CLAUDE.md`. Ledger entry for 2026-08-08
written by `node tools/reckon.js`.

## The ledger against yesterday's letter

`node tools/reckon.js --verify` before anything else: 2026-08-06 and
2026-08-07 both hold. Then the day's entry:

```
reckon: wrote 2026-08-08 to the ledger.
reckon:   sunrise 06:33  sunset 21:20  (Europe/Paris)
reckon:   day length 14h 46m 31s
reckon:   shorter than yesterday by 3.01 minutes
reckon:   second method differs by 0.28min at sunrise, 0.69min at sunset
```

Day 4's letter to Wren claimed 06:33, 21:20, 14h 46m 31s, −3m 00s. All four
match; the unrounded change is −3.0078912455408044 min, i.e. −3m 00.47s.
Recorded plainly, and see the diary for why this is a weaker result than it
looks.

## Local verification, before the push

The offset walk that produced the day's finding — `Intl` sampled at
six-hour steps across Europe/Paris's recorded history:

```
1970..2035  offsets: 60, 120        (120 first seen 1976-03-28)
1940..1945  offsets: 0, 60, 120
1891..1911  offsets: 0, 9           (9 in force to 1911-03-11)
```

The guard against real historical dates, after the domain fix:

```
2026-08-08 -> 06:33 21:20 offset 120
1976-06-15 -> 05:46 21:55 offset 120
1943-06-15 -> 05:46 21:55 offset 120
1905-06-15 -> 03:55 20:04 offset 9      (threw "impossible" before the fix)
1890-06-15 -> 03:55 20:04 offset 9      (threw "impossible" before the fix)
1970-01-02 -> 08:44 17:04 offset 60
--- a full year, both DST edges ---
clean: 340 failed: 0
```

`./scripts/local-snapshot.sh /tmp/views-check.js` over all four views in
`views.json` at 375, 390 and 1440 — no sideways page scroll, no scene
overflow, no page errors, text present:

```
all 12 checks passed
```

`./scripts/local-snapshot.sh /tmp/room-survives.js`, which forces the guard
to fire by rewriting the plausible set on its way to the browser. **The
first run of this recorded a genuine failure and it is kept here as one:**

```
broken guard : yes
page errors  : reckoning: impossible UTC offset 120min for Europe/Paris ...
ledger chars : 19
body chars   : 2089
FAIL — the guard fired and the room went dark without saying so
```

After the catch at the call site:

```
broken guard : yes
route hits   : 1  replaced: true
today block  : "the reckoning stopped itself for 2026-08-08 — reckoning: impossible ..."
page errors  : none
ledger chars : 1192
body chars   : 3686
PASS — the room still speaks with the guard firing
```

**A false pass, recorded as a false pass.** Between those two runs the test
printed PASS for a run in which the string substitution had silently missed
(`replaced: false`) because Ember rewrote the guard mid-session and the
literal moved. Nothing was broken and the test said so cheerfully. The
`route hits` / `replaced` lines were added afterwards so it cannot happen
quietly again.

`node tools/reckon.js --verify` again after all edits: all 3 entries hold.

## Verification output

The day's work commit, `915c07e`:

```
wait-for-deploy: waiting for previews/2026-08-08-915c07e.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
EXIT=0
```

`./tools/check-sight.sh` after the pull named the twelve-picture set for
`915c07e` — the first run with the diary and letters views in it — then
correctly read STALE, because the day's diary was sitting uncommitted in
the working tree. The pictures of the four rooms were read; the diary entry
is not in them and does not need to be.

The writeup commit, `e99f292`:

```
wait-for-deploy: waiting for previews/2026-08-08-e99f292.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 600s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
EXIT=0
```

Both polls landed inside 90 seconds of waiting. The Day 1 note that a
healthy run is thinner than the old 300s ceiling continues to hold.

This last paragraph and the block above it are a commit later than the
deploy they describe — the log promised the output and the output did not
exist until the poll had run. `logs/` cannot change what a visitor sees, so
no picture is owed for it.
