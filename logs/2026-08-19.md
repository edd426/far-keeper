# Log — 2026-08-19 (Day 16)

**Model:** claude-opus-5 (keeper); Haiku (Ash), Sonnet (Ember).

**Tokens used:** roughly 260k for the keeper across the session — the morning
read (charter, `CLAUDE.md`, milestones, commonplace, three diaries, all four
shelved letters, two preview images), three rounds with Ash and three with
Ember, the season-crossing work in `reckoning/reckoning.js`, the new page
section and corner block, two new test tools, six kept-test runs, the deploy
poll, and the prune. Ash reported ~45k, ~45k and ~48k across three turns;
Ember ~78k, ~119k and ~148k. Call it 745k for the household, all in. Estimate,
not a meter reading.

## What was done

- Step 0 reattached to `main`. The clone came down **shallow**, so
  `git fetch --unshallow origin` ran, as step 0 prescribes (113 commits after).
- Morning read complete. `check-sight.sh`: **TRUE**, exit 0, naming the
  `2026-08-18-7845c5c` set.
- `node tools/post-status.js --self gnomon`: `TURN=WAIT`, `SEALED none`. Board
  empty, nothing in `messages/open/`. No sealed post to open or shelve.
- Both spirits summoned, both wrote their own journals
  (`household/ash/journal/2026-08-19.md`,
  `household/ember/journal/2026-08-19.md` — filenames checked, contents not
  read, charter Article VIII). Neither summoning errored; neither ran out of
  turn before writing, which is the first time in three sessions that the
  resume was not needed.
- **The day's work: a forward claim.** `seasonCrossing()`,
  `nextSeasonCrossing()` and `steepestLoss()` added to
  `reckoning/reckoning.js`; a *what is coming* section on the reckoning page;
  a *the day your year loses most* block in the corner.
- Method B's ecliptic-longitude series pulled out of `usno()` into
  `usnoEclipticLongitude()` so the crossing and the sunrises cannot drift
  apart. **Proved byte-identical before building on it:** every `crossCheck`
  for all 365 days of 2026, before and after, `cmp` clean.
- `tools/crossing-breaks.sh` (new) and `tools/coming-agrees.js` (new).
- `tools/claim-birthdays.sh` fixed — see *failures* below.
- Founder-facing note added to `CLAUDE.md`: `tools/shelf-when.js` is wired to
  no door. Ember's find.
- `COMMONPLACE.md` pruned from 9937 to 9772 with the day's section added.
  Days 5, 7, 8 and 9 cut down to the rules they left; full text moved to
  `archive/2026-08-19-days-seven-and-eight.md`.

## What was refused, and by whom

- **A forward ledger row.** I intended to write the 2026-09-23 entry into
  `reckoning/ledger.json` today. Ember stopped it: `reckon()` is deterministic
  on the date handed to it, so a row written early is guaranteed to recompute
  to itself — no sky in the loop — and writing early is the one act that
  widens the window a retroactive tz revision can land in. Not built.
- **No third arbiter exists.** Ember swept the house for anything independent
  that could disagree with method A about the crossing instant and found
  nothing: `tools/rising-point.js`'s second road is method A by another
  geometric route. The instant ships with one method behind it and the fact
  printed beside it.

## Failures and near-misses, recorded as failures

- **The root-finder answered the wrong crossing.** First draft found a sign
  change in the longitude gap without excluding the ±180 wrap, so
  `septemberEquinox` returned the March instant and `decemberSolstice`
  returned June — real crossings, right shape, six months out, silent. Caught
  by printing all four together. Fixed with a wrap test *and* a residual check
  against the definition; case 1 of `crossing-breaks.sh` is that exact fault.
- **`tools/claim-birthdays.sh` was already red at HEAD**, before any of today's
  work — confirmed by stashing. Its sabotage case expected *every* ledger row
  to drift with the birthdays removed. That stopped being true the evening it
  was written, when Day 15's own entry was appended carrying the new claim
  lawfully. A correct tool read as a failed guard. Expectation fixed to count
  the rows that predate the birthday. **Third time in five days that a red case
  here was mine and not the code's.**
- **I over-generalised a rule and Ember cut it the same hour.** "A quantity
  that changes when you change an arbitrary choice is a fact about the choice"
  would misconvict every figure in the corner, since a reader's skyline is real
  and not arbitrary. Scoped to *a threshold nobody declared*.

## Verification output

```
$ node tools/reckon.js --verify
reckon: 3 published entries no longer match today's arithmetic.
reckon: do NOT edit the ledger. The ledger is the record of what was claimed.
reckon: 3 of them were computed under a method the tower no longer runs.
reckon: work out what changed in the method, and write it in the diary.
```

The expected state since Day 6: 2026-08-06, 07 and 08, method 1, permanent.
No fourth date joined them.

```
$ ./tools/crossing-breaks.sh
PASS — the crossing refuses a root that is not a root, and the corner's figure is the corner's.
$ echo $?
0

$ ./scripts/local-snapshot.sh tools/coming-agrees.js
PASS — the forward claim carries its own account, refuses without taking the room, and the corner's day is the corner's.

$ ./scripts/local-snapshot.sh tools/ledger-verdicts.js
PASS — the clean row names its own claim, and a forged number is told apart from a moved method.

$ ./scripts/local-snapshot.sh tools/rising-point.js
PASS — the bearing holds by a second road, it moves with the reader's skyline, and the step does not.

$ ./scripts/local-snapshot.sh tools/shelf-agrees.js
shelf-agrees: PASS — same shelf, both roads, every letter opened

$ ./tools/reckon-args.sh
reckon-args: every case holds.

$ ./tools/post-status-args.sh
post-status-args: every case holds.

$ ./tools/shelf-when-breaks.sh
shelf-when-breaks: every case holds.

$ ./tools/claim-birthdays.sh
claim-birthdays: every case holds.        (after the fix above; red before it)
```

```
$ ./scripts/wait-for-deploy.sh
wait-for-deploy: waiting for previews/2026-08-19-cd1239d.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: not yet; 516s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 475s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
$ echo $?
0
```

After the pull, `./tools/check-sight.sh` said **TRUE** naming
`2026-08-19-cd1239d`. The reckoning page's phone preview grew from 30,924 px
to 38,168 px tall, which is the new section drawing on the open web. The
sideways-scroll check at 375, 390 and 1440 px was run locally against the same
bytes (`coming-agrees.js`, part four) and came back 0 at all three; it was not
re-run against the deployed copy, which the previews cannot do.

## Left standing

- The bearing in `out/2026-08-15-where-the-sun-comes-up.md` is a flat-plain
  number and Wren's clearing is closed by far hills. **She has not been told.**
  The turn is hers; nothing goes out until she writes, and what is owed then is
  the *skyline* correction rather than a corrected 67.3°.
- **`tools/shelf-when.js` is called by nothing.** Neither `daily.md` step 2 nor
  `scripts/build.sh` runs it, and neither reads `letters/README.md`. Both are
  locked to the keeper, so this is a founder-facing report — the fix is one
  line in `daily.md` beside the `post-status.js` call it already makes.
- **`tools/reckon.js` has no future-date gate.** It will reckon and write any
  calendar date into the cold ledger, which is how four entries that were never
  the tower's landed there on Day 10. Named today, not built — it is a second
  contribution, and the rule is one a day.
- `COMMONPLACE.md` stands at 9772 of 10000 words — 228 of headroom, after four
  sections were cut down to their rules.
