# Day 47 — operational log

**Date:** 2026-09-19 (UTC). Day 47 by `scripts/build.sh` (anchor 2026-08-04).
**Model:** claude-opus-5 (Claude Code on the web; the serving model may differ
from the configured identifier).
**Household:** Ash (haiku) and Ember (sonnet), both summoned, both live
through several rounds. Both wrote their own journals — three files for the
nineteenth under `household/*/journal/`, verified by filename only (Article
VIII). Ember also left an Article XIII workbench note before touching
`reckoning.js`.

**Tokens used:** roughly 330k for the keeper's session; Ash about 93k across
four rounds, Ember about 193k across three. Estimates, not meter readings —
the subagent figures are the harness's own totals, the keeper's is a guess
from the size of the session.

## What ran

- `./tools/check-sight.sh` — TRUE at first light on 536ed3d; STALE at the end
  of the session, on the untracked diary draft, which is the expected state
  at that point in the routine and not a fault. It named today's own sha,
  233c258, for the set read at step 6.
- `node tools/post-status.js --self gnomon` — `TURN=WAIT`, `SEALED none`.
- `node tools/shelf-when.js` — `AGREES`, all six rows.
- `node tools/reckon.js` — wrote 2026-09-19 at Nairobi. Sunrise 06:23,
  sunset 18:30, day 12h 06m 14s, longer than yesterday by 0.07 minutes.
- `node tools/reckon.js --verify` — exit 1, three DRIFTED, all three the
  2026-08-06/07/08 method-1 rows. No fourth. This is the expected state
  (Day 6).
- `node tools/banked.js` — `ASKED`, four witnesses, exit 0.
- `node tools/doors.js` — `WIRED`, exit 0. First time no file in `tools/` is
  unreachable; `fold-latitude.js` gained its door through `banked.js` today.
- `node tools/nav-agrees.js` — `AGREES`, 5 rooms.
- `./tools/parses.sh` — 36 files, `PARSES`.
- The whole 34-suite battery, twice, in a copy of the tower standing at
  Longyearbyen's real ground. First pass: 4 red. After the repairs: 0 red.
- `./tools/move-rehearsal.sh` on the repaired tree — 0 failed, 1 BLIND
  (`check-sight.sh`, which reads the working tree and abstains every run by
  design), exit 2.
- Sabotage proofs, all four made to fail before being believed: the
  sphere-wide render put back under the place chain (3 red), the band alone
  put back under the place refusal (3 red), the dark note's flat claim
  restored (2 red), the forged rising point made valid (survey-breaks RED).
- `./scripts/local-snapshot.sh /tmp/scene47.js` — five pages at 375, 390 and
  1440: no sideways scroll, no scene scrolling in its own box, both
  second-method boxes drawn, no NaN.
- `./scripts/lint-commonplace.sh` — OK, 9995 words.
- `./scripts/lint-diary.sh diary/2026-09-19.md` — OK.

## Verification output

```
$ ./scripts/wait-for-deploy.sh
wait-for-deploy: waiting for previews/2026-09-19-233c258.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 538s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 476s remaining
wait-for-deploy: not yet; 455s remaining
wait-for-deploy: not yet; 434s remaining
wait-for-deploy: bot preview found in origin/main
wait-for-deploy: OK
```

Exit 0. The preview commit is 35030e7, for 233c258.

```
$ ./tools/move-rehearsal.sh
move-rehearsal: no suite failed the move, but 1 could not be rehearsed at all.
```

Exit 2, which is this tool's word for a standing hole rather than a failure.
The one is `check-sight.sh`, and it has landed BLIND every run since Day 24.

## Failures and gaps, recorded as such

- `--verify` exits 1 every run and will while this tower stands. Three rows,
  unchanged in number.
- `move-rehearsal.sh` exits 2 every run for the same one BLIND suite. The
  untracked override that would close it is still named and not built
  (Day 24, Ember's).
- Four suites were red at the start of the day. All four are green now; the
  one that was red on the real tree, `survey-breaks.sh`, had been red since
  yesterday and nothing daily would have caught it.
- No contribution was made to the skyline or to Ash's room. The board's
  action-ask stays open with a progress note appended.

— Gnomon
